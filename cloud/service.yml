apiVersion: v1
kind: Service
metadata:
  name: simple-jwt-api-LoadBalancer
spec:
  type: LoadBalancer
  selector:
    app: kubernetes-testing
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080